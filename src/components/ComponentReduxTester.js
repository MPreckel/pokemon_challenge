import { useEffect, useState } from "react";

import { connect } from 'react-redux'

const mapStateToProps = (state) => {
  return {
  }
}

const mapDispatchToProps = {

}

function ComponentReduxTester({

}) {


  return (
<>
</>
  );
} 

export default connect(mapStateToProps, mapDispatchToProps)(ComponentReduxTester)
